# Implementation Plans

Generated by the improve skill on 2026-07-11 at commit `58b33f3`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add ruff, mypy, and pre-commit tooling | P1 | S | — | TODO |
| 002 | Test CLI argument parsing | P1 | S | — | TODO |
| 003 | Deduplicate `_UNSUPPORTED_PARTITIONS` and remove dead `_partition_support` | P2 | S | — | TODO |
| 004 | Show subprocess stderr on failure in `_run` | P1 | S | — | TODO |
| 005 | Clean up misleading `partition_count` parameter in worker resolvers | P2 | S | 002 | TODO |
| 006 | Use StrEnum for build mode instead of bare string | P2 | S | 002 | TODO |
| 007 | Refactor pipeline.py into focused modules | P1 | L | 001, 002, 003, 005 | TODO |
| 008 | Replace `human_size` subprocess with pure Python | P3 | S | — | TODO |
| 009 | Fix incomplete banner string escaping | P2 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED

## Dependency notes

- **005** and **006** require **002** because they change the same `BuildOptions` surface the CLI tests cover — land the tests first.
- **007** (pipeline refactor) is the highest-risk plan. It requires **001** (typecheck safety), **002** (CLI test baseline), **003** (constants already deduped), and **005** (worker signatures already cleaned) — all to minimize risk of regressions.
- **001**, **003**, **004**, **008**, and **009** are independent and can land in any order.
- **001** (tooling) is recommended early — ruff/mypy catch issues automatically during all subsequent plans.

## Findings considered and rejected

- **"backends.py has too many responsibilities"** (303 lines) — not critically large for this tool's scope; pipeline.py is the worse offender.
- **"progress.py is too large"** (339 lines) — self-contained, well-tested, commensurate to its role.
- **"No CI pipeline"** — genuine gap but lower leverage until the test baseline is stronger (002 + 001 land first).
- **"_workspace __exit__ could delete user work_dir"** — after verification, the `work_dir is not None` guard always returns early, so user dirs are preserved.
